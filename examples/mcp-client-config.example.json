{
  "mcpServers": {
    "driftlock": {
      "command": "cargo",
      "args": [
        "run",
        "-p",
        "driftlock-mcp",
        "--",
        "stdio",
        "--repo",
        "."
      ]
    },
    "driftlock-installed": {
      "command": "driftlock-mcp",
      "args": [
        "stdio",
        "--repo",
        "."
      ]
    }
  }
}
