{
  "mcpServers": {
    "driftlock": {
      "command": "cargo",
      "args": [
        "run",
        "-p",
        "driftlock-mcp",
        "--",
        "stdio",
        "--repo",
        "."
      ]
    }
  }
}
