# Examples

See `examples/fixture-repo` for a tiny repository with an ADR, lane manifest, and expected Driftlock output.

The fixture demonstrates:

- extracting obligations from ADR text
- assigning lane metadata
- creating write/read sets
- detecting hard write conflicts
- rendering an agent brief
- verifying a patch against the work order
