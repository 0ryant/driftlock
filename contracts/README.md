# Contracts

This directory contains the public JSON Schemas for Driftlock.

Schemas are deliberately checked in instead of only generated so agents, reviewers, and downstream systems have a stable contract source before implementation details are inspected.
