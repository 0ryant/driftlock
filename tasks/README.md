# Tasks

This directory contains the canonical bootstrap task ledger.

All tasks in `taskgraph.json` are marked `complete` because this zip is a completed scaffold rather than an open project plan.

Use this ledger as the first golden artifact for Driftlock itself.
